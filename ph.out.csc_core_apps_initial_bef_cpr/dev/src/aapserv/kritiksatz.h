 #include "kritik.h"
 #include "kritikkopf.h"
 #include "kritikpos.h"
/*			#ifdef WIN32
				rc=::ServerNr(pos,fkt_nr,(void *)&s,KRITIK_BES, KRITIK_ANZ, error_msg);
			#else
				rc=::Server(fkt_nr,(void *)&s,KRITIK_BES, KRITIK_ANZ, error_msg);
			#endif
			if (rc<0) FehlerBehandlung( rc, error_msg );
			*/
/*			FehlerBehandlung( -1, "False function number!" );
			return -1;	*/
/*			return rc;	*/
